import { SeleceHeroPipe } from './selece-hero.pipe';

describe('SeleceHeroPipe', () => {
  it('create an instance', () => {
    const pipe = new SeleceHeroPipe();
    expect(pipe).toBeTruthy();
  });
});
